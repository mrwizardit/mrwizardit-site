document.addEventListener('DOMContentLoaded', () => {
    console.log("⚡ [SYSTEM MASTER]: Ritual initiated. Core protocols loaded.");

    const summonBtn = document.getElementById('summon-btn');

    summonBtn.addEventListener('click', () => {
        console.log("🔮 [SPELL]: Summoning interface requested.");
        // We'll turn this into a killer minimal modal or drop-down command terminal next
        alert("✨ TRANSMISSION INTERCEPTED // The Wizard has been notified. Prepare your network.");
    });
});
